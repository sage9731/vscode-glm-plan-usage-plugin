{
  "name": "glm-plan-usage",
  "displayName": "%displayName%",
  "description": "%description%",
  "version": "2.6.2",
  "publisher": "sagez",
  "license": "MIT",
  "icon": "assets/icon.png",
  "keywords": [
    "glm",
    "coding",
    "plan",
    "usage",
    "bigmodel",
    "zhipu"
  ],
  "homepage": "https://github.com/sage-z-cn/vscode-glm-plan-usage-plugin#readme",
  "bugs": {
    "url": "https://github.com/sage-z-cn/vscode-glm-plan-usage-plugin/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sage-z-cn/vscode-glm-plan-usage-plugin.git"
  },
  "engines": {
    "vscode": "^1.107.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "l10n": "./l10n",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "glmPlanUsage",
          "title": "%viewsContainer.title%",
          "icon": "assets/sidebar.svg"
        }
      ]
    },
    "views": {
      "glmPlanUsage": [
        {
          "id": "glmPlanUsage.usageStats",
          "type": "webview",
          "name": "%views.usageStats.name%"
        }
      ]
    },
    "commands": [
      {
        "command": "glmPlanUsage.setToken",
        "title": "%command.setToken.title%",
        "description": "%command.setToken.description%",
        "icon": "$(key)"
      },
      {
        "command": "glmPlanUsage.refresh",
        "title": "%command.refresh.title%",
        "description": "%command.refresh.description%",
        "icon": "$(refresh)"
      },
      {
        "command": "glmPlanUsage.openSettings",
        "title": "%command.openSettings.title%",
        "icon": "$(gear)"
      },
      {
        "command": "glmPlanUsage.toggleTokenUnit",
        "title": "%command.toggleTokenUnit.title%",
        "description": "%command.toggleTokenUnit.description%",
        "icon": "$(symbol-unit)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "glmPlanUsage.refresh",
          "group": "navigation@1",
          "when": "view == glmPlanUsage.usageStats"
        },
        {
          "command": "glmPlanUsage.setToken",
          "group": "navigation@2",
          "when": "view == glmPlanUsage.usageStats"
        },
        {
          "command": "glmPlanUsage.toggleTokenUnit",
          "group": "1_actions@1",
          "when": "view == glmPlanUsage.usageStats"
        },
        {
          "command": "glmPlanUsage.openSettings",
          "group": "1_actions@2",
          "when": "view == glmPlanUsage.usageStats"
        }
      ]
    },
    "configuration": {
      "title": "%configuration.title%",
      "properties": {
        "glmPlanUsage.authToken": {
          "type": "string",
          "default": "",
          "description": "%configuration.authToken.description%",
          "deprecationMessage": "%configuration.authToken.deprecated%"
        },
        "glmPlanUsage.baseUrl": {
          "type": "string",
          "default": "https://open.bigmodel.cn/api/anthropic",
          "enum": [
            "https://open.bigmodel.cn/api/anthropic",
            "https://dev.bigmodel.cn/api/anthropic",
            "https://api.z.ai/api/anthropic"
          ],
          "enumDescriptions": [
            "%configuration.baseUrl.enum.zhipu%",
            "%configuration.baseUrl.enum.dev%",
            "%configuration.baseUrl.enum.zai%"
          ],
          "description": "%configuration.baseUrl.description%"
        },
        "glmPlanUsage.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "%configuration.autoRefresh.description%"
        },
        "glmPlanUsage.refreshInterval": {
          "type": "number",
          "default": 300,
          "description": "%configuration.refreshInterval.description%"
        },
        "glmPlanUsage.afkThreshold": {
          "type": "number",
          "default": 1800,
          "description": "%configuration.afkThreshold.description%"
        },
        "glmPlanUsage.enableRetry": {
          "type": "boolean",
          "default": true,
          "description": "%configuration.enableRetry.description%"
        },
        "glmPlanUsage.resetNotification": {
          "type": "boolean",
          "default": true,
          "description": "%configuration.resetNotification.description%"
        },
        "glmPlanUsage.tokenUnit": {
          "type": "string",
          "enum": [
            "si",
            "chinese"
          ],
          "enumDescriptions": [
            "%configuration.tokenUnit.enum.si%",
            "%configuration.tokenUnit.enum.chinese%"
          ],
          "default": "si",
          "description": "%configuration.tokenUnit.description%"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "package": "npm run compile && vsce package --out ./release/",
    "publish": "npm run compile && npx @vscode/vsce publish",
    "build-and-install": "node scripts/build-and-install.js"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
